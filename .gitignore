# OS / editor noise only. Everything else is intentionally committed.
.DS_Store
Thumbs.db
desktop.ini
*.swp

# physical-size walker runtime artifacts
physical-size/walker-errors.log
physical-size/failed-batches/
